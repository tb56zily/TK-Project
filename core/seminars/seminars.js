'use strict';

/* Controllers */
angular.module('myApp.seminars', ['ngRoute']).controller('SeminarsCtrl', [
  '$scope', '$location', '$http', 'localStorageService' ,function($scope, $location, $http, localStorageService) {



  }
]);
